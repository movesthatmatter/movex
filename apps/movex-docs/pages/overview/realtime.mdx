# Realtime State Sync

Movex works with the Obervability pattern accross all the peers(clients) subscribed to a particular Resource State. This means that as soon as the state gets updated, all of the peers in the network will are notified, and the next state gets reprocessed.