import { Hero } from '../modules/home/Hero.tsx';
import Link from 'next/link';

<div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)]">

{/* <h1 className="text-3xl font-bold underline">Hello world!</h1> */}

{/* works */}

<Hero />

import { features, faqs, community } from '../modules/home/homeVars';

<div className="mx-auto mt-16 max-w-2xl mt-20 lg:mt-24 lg:max-w-4xl mb-20 lg:mb-24">
  <dl className="grid max-w-xl grid-cols-1 gap-x-8 gap-y-10 lg:max-w-none lg:grid-cols-2 lg:gap-y-16">
    {features.map((feature) => (
      <div key={feature.name} className="relative pl-16">
        <dt className="text-base font-semibold leading-7 text-gray-900 dark:text-gray-300">
          <div
            className={`absolute left-0 top-0 flex h-10 w-10 items-center justify-center rounded-lg bg-purple-500`}
          >
            <feature.icon className="h-6 w-6 text-white" aria-hidden="true" />
          </div>
          {feature.name}
        </dt>
        <dd className="mt-2 pb-3 text-base leading-7 text-gray-600 dark:text-gray-400">
          {feature.description}
        </dd>
        <Link
          className="hover:underline text-gray-700 dark:text-gray-300"
          href={feature.link.url}
        >
          {feature.link.label}
        </Link>
      </div>
    ))}
  </dl>
</div>

</div>

<div id="examples" className="h-screen w-full" style={{
  background: '#FFEA00',
}}>

  <div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)] p-16">
    <div style={{
      height: 200,
    }} className="mb-10">
      <h1 className="text-7xl text-black font-bold">
        Try Movex with a Game of
        <span className="text-purple-500">
          <span className="text-reds-500">Rock </span>
          <span className="text-green-5002">Paper </span>
          <span className="text-blue-5002">Scissors</span>
        </span>
      </h1>
    </div>

    <iframe src="http://localhost:4200/local/rps" className="w-full h-full" style={{
      height: 500
    }} />

  </div>
</div>

<div className=" w-full bg-pink-500" style={{
      background: 'rgba(46,5,116,1)',
}}>

  <div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)] p-16">
    <div className="mb-20">
      <h1 className="text-7xl font-bold">Or a game of Maha Chess</h1>
    </div>

    <div className="flex">
      <iframe src="http://localhost:4200/match:83d0da5e-8b93-4c6d-94db-2a3d594f33e7?color=white" className="w-full h-full" style={{
        height: 860,
      }} />

      <iframe src="http://localhost:4200/match:83d0da5e-8b93-4c6d-94db-2a3d594f33e7?color=white" className="w-full h-full" style={{
        height: 860,
      }} />
    </div>

  </div>
</div>

<div id="faqs" className="w-full">
  <div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)] p-16 pt-20">
    <h1 className="text-5xl font-bold mb-20 text-center">FAQs</h1>
    <div className="grid grid-cols-2 gap-20">
      {faqs.map((faq) => (
        <div
          key={faq.q}
          className="relative mb-10 pb-6 border-b border-slate-700"
        >
          <dt className="text-lg font-semibold leading-7 text-gray-900 dark:text-gray-300">
            {faq.q}
          </dt>
          <dd className="mt-2 pb-3 text-base leading-7 text-gray-600 dark:text-gray-400">
            {faq.a}
          </dd>
          {faq.link && (
            <Link
              className="hover:underline text-gray-700 dark:text-gray-300"
              href={faq.link.url}
            >
              {faq.link.label}
            </Link>
          )}
        </div>
      ))}
    </div>
  </div>
</div>

<div id="community" className="w-full">
  <div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)] p-16 pt-20">
    <h1 className="text-5xl font-bold mb-20 text-center">Community</h1>

      <div className="flex w-full gap-40">
        {community.map((c) => (
          <div className="flex-1 text-center bg-opacity-10 hover:bg-opacity-20 bg-red-100 rounded-lg p-5">
            <h4 className="text-lg font-bold">{c.name}</h4>
          </div>
        ))}
    </div>

  </div>
</div>
