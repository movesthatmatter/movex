import { Hero } from '../modules/home/Hero.tsx';
import Link from 'next/link';

<div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)]">

{/* <h1 className="text-3xl font-bold underline">Hello world!</h1> */}

{/* works */}

<Hero />

import { features, faqs, community } from '../modules/home/homeVars';

<div className="mx-auto mt-16 max-w-2xl mt-20 lg:mt-24 lg:max-w-4xl mb-20 lg:mb-24">
  <dl className="grid max-w-xl grid-cols-1 gap-x-8 gap-y-10 lg:max-w-none lg:grid-cols-2 lg:gap-y-16">
    {features.map((feature) => (
      <div key={feature.name} className="relative pl-16">
        <dt className="text-base font-semibold leading-7 text-gray-900 dark:text-gray-300">
          <div
            className={`absolute left-0 top-0 flex h-10 w-10 items-center justify-center rounded-lg bg-purple-500`}
          >
            <feature.icon className="h-6 w-6 text-white" aria-hidden="true" />
          </div>
          {feature.name}
        </dt>
        <dd className="mt-2 pb-3 text-base leading-7 text-gray-600 dark:text-gray-400">
          {feature.description}
        </dd>
        <Link
          className="hover:underline text-gray-700 dark:text-gray-300"
          href={feature.link.url}
        >
          {feature.link.label}
        </Link>
      </div>
    ))}
  </dl>
</div>

</div>
{/* ?fontsize=14&hidenavigation=1&theme=dark&codemirror=1&editorsize=0&view=split&hidedevtools=1 */}
<div id="examples" className="h-screen w-full bg-gradient-to-tr from-movexBlue-400 via-indigo-500 to-purple-500 p-6 lg:pt-0 lg:p-20 lg:pt-0 flex flex-col">
  <h2 className="text-4xl lg:text-7xl font-bold pt-20 pb-6">Play Rock Paper Scissors</h2>
  <div className="flex-1">
    <iframe src="https://codesandbox.io/embed/rps-demo-x877yl?view=preview&module=%2Fsrc%2FGame.tsx"
      className="hidden lg:block"
      style={{
        width: ' 100%',
        height: '100%',
        borderRadius: '8px',
        overflow: 'hidden',
        borderRight: 'solid black 16px',
      }}
      title="rps-demo"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
    />
    <iframe src="https://codesandbox.io/embed/rps-demo-x877yl?view=preview"
      className="lg:hidden"
      style={{
        width: ' 100%',
        height: '100%',
        borderRadius: '8px',
        overflow: 'hidden',
        borderRight: 'solid black 16px',
      }}
      title="rps-demo"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
    />
   </div>
</div>

{/* // import { Game } from '../modules/home/game/Game'; */}

<div id="faqs" className="w-full">
  <div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)] p-16 pt-20">
    <h1 className="text-5xl font-bold mb-20 text-center text-green-500">
      FAQs
    </h1>
    <div className="grid grid-cols-2 gap-20">
      {faqs.map((faq) => (
        <div
          key={faq.q}
          className="relative mb-10 pb-6 border-b border-slate-700"
        >
          <dt className="text-lg font-semibold leading-7 text-gray-900 dark:text-gray-300">
            {faq.q}
          </dt>
          <dd className="mt-2 pb-3 text-base leading-7 text-gray-600 dark:text-gray-400">
            {faq.a}
          </dd>
          {faq.link && (
            <Link
              className="hover:underline text-gray-700 dark:text-gray-300"
              href={faq.link.url}
            >
              {faq.link.label}
            </Link>
          )}
        </div>
      ))}
    </div>
  </div>
</div>

<div id="community" className="w-full">
  <div className="container mx-auto max-w-[90rem] gap-2 pl-[max(env(safe-area-inset-left),1.5rem)] pr-[max(env(safe-area-inset-right),1.5rem)] p-16 pt-20">
    <h1 className="text-5xl font-bold mb-6 text-center text-pink-500">Community</h1>
    <h3 className="text-xl mb-20 text-center text-slate-300">Join our community to get help, share your projects, and even contribute to the project!</h3>

      <div className="flex w-full gap-20">
        <div />
        <div />
        {community.map((c) => (
          <Link href={c.link} target="_blank" className="flex-1 text-center bg-opacity-10 hover:bg-opacity-20 bg-slate-500 rounded-lg p-5 border border-slate-500 border-opacity-30">
            <h4 className="text-lg font-bold mb-2">{c.name}</h4>
            <p>{c.desc}</p>
          </Link>
        ))}
        <div />
        <div />
    </div>

  </div>
</div>
