import * as SpecUtils from 'movex-specs-util';

export const reducer = SpecUtils.speedPushGameReducer;
export const initialState = SpecUtils.initialSpeedPushGameState;

export type SpeedGameState = SpecUtils.SpeedGameState;
